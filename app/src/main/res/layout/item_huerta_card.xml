<?xml version="1.0" encoding="utf-8"?>
<!-- Tarjeta de huerta del Home (PLAN_MEJORAS_20.md, punto 7): antes se
     armaba por código en MapaHuertasActivity.renderizarTarjetas(), ahora
     la infla HuertaCardAdapter dentro de un RecyclerView. Los valores que
     cambian según el estado real de la huerta (colores, textos, tint) se
     setean en el bind() del ViewHolder; lo que es siempre igual queda acá. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/wrapper"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:orientation="horizontal"
    android:background="@drawable/bg_card_huerta"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:clipToOutline="true">

    <View
        android:id="@+id/franjaEstado"
        android:layout_width="4dp"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="@dimen/simona_space"
        android:paddingTop="14dp"
        android:paddingEnd="@dimen/simona_space"
        android:paddingBottom="14dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvNombre"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/simona_tinta"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="Huerta de tomates" />

                <TextView
                    android:id="@+id/tvRango"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingTop="3dp"
                    android:textColor="@color/simona_tinta_suave"
                    android:textSize="12sp" />
            </LinearLayout>

            <ImageView
                android:id="@+id/ivMas"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="@dimen/simona_space_sm"
                android:src="@drawable/ic_more_vert"
                app:tint="@color/simona_tinta_suave"
                android:foreground="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_chevron_right"
                app:tint="@color/simona_azul"
                android:importantForAccessibility="no" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingTop="10dp">

            <ImageView
                android:id="@+id/ivEstadoIcono"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="6dp"
                android:importantForAccessibility="no" />

            <TextView
                android:id="@+id/tvEstado"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingStart="2dp"
                android:paddingTop="@dimen/simona_space_xxs"
                android:paddingEnd="2dp"
                android:paddingBottom="@dimen/simona_space_xxs"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
